// Arithmetic operators - Addition
// In the code below we calculate the sum of numOne and numTwo with the Addition operator +. 
// Then, the console.log() statement checks whether the sum equals 10. Change the code so that the console.log() statement logs true.

// let numOne = 3;
// let numTwo = 7;

// console.log(numOne + numTwo === 10 );

// Arithmetic operators - Subtraction
// In the code below we calculate the difference of numOne and numTwo with the Substraction operator -. 
// Then, the console.log() statement checks whether the difference equals 5. Change the code so that the console.log() 
// statement logs true.

// Arithmetic operators - Exponential
// In the code below we calculate numOne to the power of numTwo with the Exponential operator **.
//  Then, the console.log() statement checks whether the result of this calculation equals 8. Change
//   the code so that the console.log() statement logs true.

// let numOne = 3;
// let numTwo = 2;

// console.log(numTwo ** numOne === 8 );

// Arithmetic operators - Remainder
// In the code below we use the Remainder operator to calculate the remainder of numOne divided by numTwo.
//  The console.log() statement expects the remainder to be 3. Change the code so that the remainder is 3.


let numOne = 18;
let numTwo = 5;

console.log(numOne % numTwo === 3 );

